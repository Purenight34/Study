알고리즘 각 폴더는 아래 예시와 같은 구조로 만들어집니다.

```text
각각 알고리즘 이름/
├── pseudocode.md
├── solution.py   # 또는 .cpp, .java 등 문제 담당자가 제공하는 예시
└── tests/
    ├── input01.txt
    ├── output01.txt
    ├── input02.txt
    └── output02.txt
```

`pseudocode.md`는 문제 담당자가 알고리즘을 설명하기 위한 핵심 문서입니다.

문서는 3개의 영역으로 구성합니다.

#### 1. 문제 설명 및 입출력 정의

문제에서 해결해야 하는 내용을 간단히 설명하고, 프로그램이 받아야 하는 입력(Input) 과 출력해야 하는 결과(Output) 를 명확하게 정의합니다.

```text
이 문제는 주어진 숫자를 오름차순으로 정렬하는 문제이다.
N(N < 100) 개의 숫자가 공백을 두고 입력된다.
오름차순으로 정렬된 N 개의 숫자를 출력한다.
```

---

#### 2. 알고리즘 의사코드

문제를 해결하기 위한 과정을 특정 프로그래밍 언어의 문법이 아닌 **의사코드(Pseudocode)** 로 작성합니다.

의사코드는 C++, Python, Java 등 어떤 언어를 사용하는 사람도 알고리즘의 흐름을 이해할 수 있도록 작성하는 것을 원칙으로 합니다.

printf("%d", a);
cout << a;
print(a)

a 출력 < 우리가 쓰는 말로 코딩 해보는게 의사 코드다.

그런데? 조심해야 할 점
-> 나중에 익숙해지면 의사 코드 한 줄 범위를 되게 넓게 잡는게 가능

메인 함수
    학생들 점수 입력받기 
    가장 높은 점수 찾기 -> 이런거 하나 하나를 더 작게 직접 짜보기
    가장 높은 점수 출력하기
메인 함수 끝


최댓값 찾기의 알고리즘 의사코드 

메인 함수
    학생들 점수 입력받기 -> a = list(map(int, input().split('')))
    
    낮은 점수 값 max_score 선언 -> max_score = -1
    for 학생 점수               -> for person in people :
        if max_score보다 학생 점수가 크면 -> if ma
            max_score에 학생 점수 넣기

    max_score 출력하기
메인 함수 끝


```text

Input
  ↓
알고리즘 처리 과정
  ↓
Output
```

> **중요:** 실제 코드 작성보다 먼저, 문제를 어떤 순서와 논리로 해결할 것인지를 설명할 수 있어야 합니다.

---

#### 3. 테스트 케이스 설명

`tests/` 폴더에 작성한 각 입력 파일이 **어떤 상황을 검증하기 위한 테스트인지** 설명합니다.

단순한 정상 입력뿐만 아니라 담당자가 직접 알고리즘의 **경계 조건(Edge Case)**, **예외적인 입력**, **구현 시 실수하기 쉬운 부분**을 고민하여 테스트 케이스를 구성합니다.

예시:
| 테스트 파일        | 확인하려는 요소                 |
| :------------ | :----------------------- |
| `input01.txt` | 일반적인 입력에서 정상적으로 동작하는지 확인 |
| `input02.txt` | 입력 크기가 최소인 경우 확인         |
| `input03.txt` | 동일한 값이 여러 개 존재하는 경우 확인   |
| `input04.txt` | 이미 정답 조건을 만족하는 입력 확인     |
| `input05.txt` | 알고리즘의 경계 조건 및 인덱스 처리 확인  |

> 모든 문제에 동일한 테스트 조건을 적용할 필요는 없습니다.
각 테스트는 입력 파일과 예상 출력 파일을 한 쌍으로 구성합니다.


#### 스터디 방법
각 스터디원은 각자 맡은 문제/알고리즘의 의사코드를 작성하고, 공부해와서 돌아가는 코드 파일과 인풋/아웃풋 예시를 작성한다.
각자 맡은 문제의 의사코드를 설명한다. 설명을 들은 나머지 스터디원은 의사코드만 보고, 자기가 돌아가는 파일을 만들어서 담당자가 제시한 인풋/아웃풋을 테스트한다.
이 테스트 과정에서 생기는 궁금점이나 문제점이 있다면 문제 담당자한테 문의한다.

요약
1. 각자 문제를 하나씩 담당한다. -> 일단 초기에는 저 박은하가 배정해드립니다.
2. 담당한 문제를 공부하고, 의사코드를 작성한다.
3. 작성한 의사코드가 맞는지는 혼자서 잘 확인해보고(Ai의 도움을 써도, 이해만 하면 됩니다.)
4. 진짜 돌아가는 .py, .java, .cpp, .c 등의 파일로 만들어서 확인합니다.
5. 그리고 문제화 합니다. -> 입력와 출력을 명확하게 제시하고, .txt 파일로 대응되게 만들기.(파일 입출력은 안해도 됩니다. 어차피 공부용이라서, 직접 하나하나 확인해보고 맞으면 Ok라서 맞고 틀리고를 기록할 필요 없음. 안하면 자기가 손해기 때문에, 채점은 담당자가 제시한 output을 보고 스스로 합니다.)
6. 단, 남의 담당 문제를 채점하는게 자기 몫인거지, 자기가 담당한 문제 유기하면 안됩니다. 그건 다른 사람이 풀어보고 담당자에게 질문했을 때, 답해줄 수 있어야 해요. 자기가 담당한 문제는 꼭 이해해오기!
7. 스터디 시간이 되면, 배정 받은 문제의 의사코드를 발표하고 질문을 받습니다. 질문에 답해주고, 다른 사람의 설명을 듣습니다. -> 이때, 자기가 만든 코드 보여주면서 돌아가는거 확인시켜주셔야 합니다.