# 📘 Study Rules

이 스터디는 알고리즘을 **이해 → 의사코드로 설명 → 구현 → 테스트**을 목표로 합니다.

---

# 1. 문제 폴더 구조

각 알고리즘 문제는 다음과 같은 구조로 작성합니다.

```text
알고리즘 이름/
├── pseudocode.md
├── solution.py       # 또는 .cpp, .java, .c 등 작동 가능한 언어
└── tests/
    ├── input01.txt
    ├── output01.txt
    ├── input02.txt
    └── output02.txt
```

* `pseudocode.md` : 문제 설명, 의사코드, 테스트 케이스 설명
* `solution.*` : 문제 담당자가 작성한 실제 동작 코드 -> 남들이 스스로 코드 짤 수 있도록 git에는 올리지 말기
* `tests/` : 문제를 검증하기 위한 입력과 예상 출력

`solution`의 프로그래밍 언어는 담당자가 자유롭게 선택합니다.

각 테스트는 입력과 출력이 한 쌍이 되도록 작성합니다.

```text
input01.txt ↔ output01.txt
input02.txt ↔ output02.txt
```

---

# 2. `pseudocode.md` 작성 규칙

`pseudocode.md`는 문제 담당자가 해당 알고리즘을 다른 스터디원에게 설명하기 위한 핵심 문서입니다.

문서는 다음 **3개의 영역**으로 작성합니다.

## 2-1. 문제 설명 및 입출력 정의

해결해야 하는 문제와 프로그램의 Input / Output을 명확하게 작성합니다.

예시:

```text
N개의 숫자를 오름차순으로 정렬한다.

Input
N개의 정수가 공백으로 구분되어 입력된다.

Output
오름차순으로 정렬된 N개의 정수를 출력한다.
```

다른 사람이 이 설명만 보고도 프로그램을 작성할 수 있을 정도로 명확하게 작성합니다.

---

## 2-2. 알고리즘 의사코드

특정 프로그래밍 언어의 문법이 아닌 **의사코드(Pseudocode)** 로 해결 과정을 작성합니다.

예를 들어

```text
printf("%d", a);
cout << a;
print(a)
```

와 같이 언어마다 다른 코드는 의사코드에서는

```text
a를 출력한다
```

처럼 표현합니다.

즉, **우리가 사용하는 말로 프로그램의 동작 순서를 작성한다**고 생각하면 됩니다.

예를 들어 최댓값을 찾는 알고리즘은 다음처럼 작성할 수 있습니다.

```text
max_score를 매우 작은 값으로 설정한다

각 학생의 점수를 확인한다
    현재 점수가 max_score보다 크다면
        max_score를 현재 점수로 변경한다

max_score를 출력한다
```

처음에는 알고리즘의 동작을 가능한 한 작은 단계로 나누어 작성합니다.

알고리즘에 익숙해지면 의사코드 한 줄의 범위를 넓힐 수 있지만, 현재 공부하려는 핵심 알고리즘 자체를 한 줄로 생략하지 않는 것을 원칙으로 합니다.

> 실제 코드를 작성하기 전에 문제를 어떤 순서와 논리로 해결할 것인지 설명할 수 있어야 합니다.

---

## 2-3. 테스트 케이스 설명

`tests/`에 작성한 각 테스트가 무엇을 확인하기 위한 것인지 설명합니다.

| 테스트 파일        | 확인하려는 요소          |
| ------------- | ----------------- |
| `input01.txt` | 일반적인 입력         |
| `input02.txt` | 최소 입력             |
| `input03.txt` | 동일한 값이 존재하는 경우    |
| `input04.txt` | 이미 정답 조건을 만족하는 경우 |
| `input05.txt` | 경계 조건 및 구현 실수 확인  |

모든 문제에 같은 테스트를 만들 필요는 없습니다.

담당자가 해당 알고리즘의

* 일반적인 입력
* 경계 조건(Edge Case)
* 예외적인 상황
* 구현할 때 실수하기 쉬운 부분

등을 직접 고민하여 테스트 케이스를 구성합니다.

---

# 3. 스터디 진행 방법

### 1. 문제 배정

각 스터디원은 하나의 문제를 담당합니다.

초기 문제는 제가 배정해드립니다.

---

### 2. 담당 문제 준비

문제 담당자는 스터디 전까지 다음 내용을 준비합니다.

```text
문제 및 알고리즘 공부
        ↓
pseudocode.md 작성
        ↓
실제 코드 구현
        ↓
테스트 케이스 작성
```

작성한 의사코드가 올바른지는 AI등을 통해 검증해봅시다.
AI에게 질문 던져보라고 하고 대답해서 자가채점 하는 방식도 괜찮습니다.
결국 최종적으로 자신이 구현, 설명할 수 있다면 괜찮습니다. 

---

### 3. 담당 문제 발표

스터디 시간에는 담당자가 `pseudocode.md` 와 직접 짠 코드를 기준으로 자신의 문제를 설명합니다.

담당자는

* 문제의 Input / Output
* 알고리즘의 동작 과정
* 의사코드
* 구현 시 주의할 점
* 테스트 케이스

를 설명하고, 자신이 작성한 `solution` 코드가 실제로 동작하는 것도 확인합니다.

---

### 4. 다른 스터디원이 직접 구현

나머지 스터디원은 **담당자의 실제 코드를 복사하지 않고, 의사코드를 보고 자신의 언어로 직접 구현합니다.**

사용 언어는 자유입니다.

구현 후 담당자가 제공한 `inputXX.txt`를 넣어보고 결과가 `outputXX.txt`와 같은지 직접 확인합니다.

별도의 자동 채점이나 파일 입출력을 구현할 필요는 없습니다. 공부용이니까요.

---

### 5. 질문 및 피드백

구현이나 테스트 중 궁금한 점, 오류, 추가하면 좋을 테스트 케이스가 있다면 해당 문제 담당자에게 질문합니다.

**문제 담당자는 자신의 문제를 다른 사람이 질문했을 때 설명할 수 있어야 합니다!**
